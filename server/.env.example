# MongoDB connection string (matches MongoDB Compass default).
# The database name at the end ("portfolio") is where enquiries are stored.
MONGODB_URI=mongodb://localhost:27017/portfolio

# Port the API server listens on.
PORT=5000
